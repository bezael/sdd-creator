# Specs Index — bezael/sdd-creator

> Dominicode SDD project memory. **One file, version-controlled, maintained by the agent.**
> Lives at `specs/INDEX.md`, above the per-feature folders. The agent reads it at Step 0.5 (to avoid duplicating work and to reuse decisions) and updates it at hand-off (Step 5).
> This file is **committed** — unlike `specs/**/.work/`, it is durable memory, not scratch.

---

## Shared decisions

| Decision | Value | Rationale (1 line) | First decided in |
|----------|-------|--------------------|------------------|
| CBRM vocabulary | Contract (criteria) → Lane (harness, conventions, boundaries) → Verdict (evidence) | One name per piece across both skills, templates and READMEs | `contract-based-review` |
| Root `AGENTS.md` in target repos | Reserved for the lane generated by `dominicode-harness-init`; non-Claude installs put skills under `.agents/<skill>/` | Otherwise the SDD instructions and the lane compete for the same file, and the skills' `templates/` overwrite each other | `contract-based-review` |
| Light contract location | Template at `.dominicode/spec.template.md`, filled copies at `.dominicode/specs/<issue-number>-<slug>.md` | Small Issues need a contract even in SDD repos; a template filled in place is lost on the next Issue | `contract-based-review` |
| Credits | SDD: "SDD adaptation by"; CBRM: "CBRM by Bezael Pérez · Dominicode" | SDD is an industry methodology; CBRM is a named method of its own | `contract-based-review` |
| Line endings | LF everywhere, enforced by `.gitattributes` | A CRLF conversion inflated the 1.8.0 diff 2.5× and broke `git blame` | `contract-based-review` |

---

## Specs

| Slug | Vision (1 line) | Status | Key stack | Related specs |
|------|-----------------|--------|-----------|---------------|
| `contract-based-review` | Name CBRM and ship `dominicode-harness-init` so an Issue becomes a verified PR through contract, lane and verdict (plan-only change, no `spec.md`/`tasks.md`) | completed | Markdown skills, `plugin.json` | — |

---

*Maintained by the **dominicode-sdd-creator** skill (Bezael Pérez · Dominicode). Do not hand-edit during an active spec session — let the skill reconcile it at hand-off.*
